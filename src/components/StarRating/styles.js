import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  container: {
    flex: 1,
    flexDirection: 'row',
    marginBottom: 30
  },

  starButton: {
    paddingLeft: 5,
    paddingRight: 5
  }
})

export default styles
